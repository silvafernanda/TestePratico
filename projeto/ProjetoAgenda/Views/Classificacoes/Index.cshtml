@model IEnumerable<ProjetoAgenda.Models.Classificacoes>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.NomeClassificacao)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.NomeClassificacao)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.IdClassificacao }) |
            @Html.ActionLink("Details", "Details", new { id=item.IdClassificacao }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.IdClassificacao })
        </td>
    </tr>
}

</table>
